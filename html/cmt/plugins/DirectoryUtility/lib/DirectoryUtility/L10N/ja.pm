package DirectoryUtility::L10N::ja;

BEGIN { require utf8 if MT->version_number =~ /^5/; }

use strict;
use base 'DirectoryUtility::L10N::en_us';
use vars qw( %Lexicon );

%Lexicon = (
 'MT Tag of the category and the folder is enhanced.' => 'カテゴリとフォルダタグを拡張します。',
);

1;