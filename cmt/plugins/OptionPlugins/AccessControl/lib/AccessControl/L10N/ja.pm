# $Id$

package AccessControl::L10N::ja;

use strict;
use base 'AccessControl::L10N::en_us';
use vars qw( %Lexicon );

## The following is the translation table.

%Lexicon = (

'if there is no reference to autheority sites,sites redirect to an authoritative reference.'
  => 'サイトへの参照権限がない場合は、参照権限のあるサイトへリダイレクトする。',

);

1;
